import React, { Component } from 'react';

class Footer extends Component {
  render() {
    return (
      <footer className='component'>this is the footer</footer>
    );
  }
}

export default Footer;
