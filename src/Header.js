import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header className='component'>this is the header</header>
    );
  }
}

export default Header;
